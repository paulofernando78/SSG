{
  "name": "boilerplate-ssg",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "npx serve dist",
    "deploy": "git add . && git commit -m \"wip\" && git push"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
